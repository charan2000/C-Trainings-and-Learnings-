#include <bits/stdc++.h>
using namespace std;

void reverse(int a)
{
    if (a == 0)
    {
        return;
    }
}

int main()
{
    int n;
    cout << "Enter a no. : \n";
    cin >> n;
    reverse(n);
}